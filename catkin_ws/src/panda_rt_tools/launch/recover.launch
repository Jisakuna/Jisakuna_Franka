<launch>
  <arg name="robot_ip" default="192.168.1.2"/>

  <!-- Perform automatic error recovery (clears a Reflex / motion errors). -->
  <node name="panda_recover" pkg="panda_rt_tools" type="recover"
        output="screen" args="$(arg robot_ip)"/>
</launch>
